import { render } from '@/tests/test-utils';

import { AreaInputGroup } from '.';

describe('AreaInputGroup', () => {
  it('should render the heading', () => {
    render(<AreaInputGroup line={undefined} />);

    // Assert
  });
});
