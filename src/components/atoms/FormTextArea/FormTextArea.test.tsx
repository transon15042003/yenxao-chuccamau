import { render } from '@/tests/test-utils';

import { FormTextArea } from '.';

describe('FormTextArea', () => {
  it('should render the heading', () => {
    render(<FormTextArea line={undefined} />);

    // Assert
  });
});
