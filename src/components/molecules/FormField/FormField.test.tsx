import { render } from '@/tests/test-utils';

import { FormField } from '.';

describe('FormField', () => {
  it('should render the heading', () => {
    render(<FormField label="" type="text" />);

    // Assert
  });
});
